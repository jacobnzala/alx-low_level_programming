#include "main.h"
/**
*main - check the code
*@x: int
*@y: int
*Return: Always 0.
*/

int test(x, y){
	if (y > 0)
		return -1;
}

int main (void)
	x = 10;
	y = 2;
	pow(x, y);
	printf("the power is %d\n", pow);

