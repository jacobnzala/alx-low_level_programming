#include <stdio.h>
#include <stdlib.h>
#include <main.h>

/**
 * main - print -putchar, followed by a new line
 * Return: Always 0 (sucess)
 */




int main(void)
{       putchar(-);
	putchar(p);
	putchar(u);
	putchar(t);
	putchar(c);
	putchar(h);
	putchar(a);
	putchar(r);
	putchar(\n);
        return(0);
}
